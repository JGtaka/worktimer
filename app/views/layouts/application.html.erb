<!DOCTYPE html>
<html>
  <head>
    <title>Worktimer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :page_stylesheet %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

  <nav class="navbar navbar-expand-lg ">
  <div class="container-fluid">
      <a class="navbar-brand" href="#">WorkTimer</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
    <% if user_signed_in? %>
      <a class="navbar-brand" href="#"><%= current_user.email %></a>
    <% end %>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">サービスについて</a>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link" href="<%= root_path %>">マイページ</a>
          </li>
        <% else %>
          <li class="nav-item">
            <a class="nav-link" href="<%= new_user_registration_path %>">新規登録</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= new_user_session_path %>">ログイン</a>
          </li>
        <% end %>
      </ul>
      <span class="navbar-text">
      </span>
    </div>
  </div>
</nav>

<nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
  <ol class="breadcrumb">
    <%= yield :breadcrumbs %>
  </ol>
</nav>

  <% if user_signed_in? %>
  <% end %>

    <div class="main-content-wrapper" style="min-height: 70vh;">
      <%= yield %>
    </div>

    <hr style="margin:100;">

    <footer class="footer bg-light text-center py-3">
      <div>
        <a href="mailto:info@example.com" class="mx-2">お問い合わせ</a>
        <a href="https://twitter.com/your_twitter" target="_blank" rel="noopener" class="mx-2">Twitter</a>
      </div>
      <div class="text-muted small mt-2">&copy; <%= Time.current.year %> WorkTimer</div>
    </footer>
  </body>



    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

  

  </body>

  
</html>
